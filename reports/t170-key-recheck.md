# 鍵を置いたあとの再確認（t170・**$0**）

生成: **2026-09-23T20:29:26+0900**

**このタスクは API を 1 回も呼ばない。** 在るかどうかを見るだけ。
**鍵そのものは出さない。** 在る／無い・どの経路・長さの桁数だけ。

| 経路 | 結果 | 長さ | 備考 |
| --- | --- | --- | --- |
| 環境変数 | ❌ 無い | — | この周回のシェルに在るか |
| `~/openclaw/config/.env` | ✅ **在る** | 3 桁 | **推奨の置き場** |
| `launchctl getenv` | ❌ 無い | — | **rc は見ていない**（未設定でも 0 を返すため） |
| launchd の plist | ❌ 無い | — | 記述は無い |

- `.env` のパーミッション: **-rw-------**（`-rw-------` が望ましい）

- ジョブは **載っている**: `	state = not running 	runs = 3 	last exit code = 0 		state = active 		state = active `

## 選定が通るか（`--dry-run`・**API は呼ばない**）

```
対象: mens-hairremoval-comparison-2026（23537 文字 / 最終確認 未）
--dry-run のため API は呼ばない。ここで終わり。
```

- 終了コード **0**（**`rc=0` は動いた証拠でしかない**・最上位ルール 13）

---

**読み方**

- どこかに ✅ が 1 つでもあれば、**鍵は置かれている。**
  それでも動いていないなら原因は鍵ではない（次はジョブのログを見る）
- 全部 ❌ なら、**まだ置かれていない。** `docs/refresh-api-key.md` の手順を実行する
- **`rc=0` は証拠にならない**（最上位ルール 13）。上の表は**長さ**で判定している
