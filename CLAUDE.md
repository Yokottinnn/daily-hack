# CLAUDE.md

## 最上位ルール

他のどの指示よりも優先する。

### 1. 終わっていない作業があるなら、次の一手をダイアログで出す（例外なし）

**判定の基準は「選択肢を書いたかどうか」ではない。「まだ終わっていない作業があるかどうか」。**

未完了の作業が 1 件でも残った状態でターンを終えるなら、**必ず `AskUserQuestion` の
ダイアログで次の一手を提示する。** 本文に書くのは報告だけにする。

利用者に選ばせるときも同じ。本文に選択肢を書き並べず、必ずダイアログで提示する。
「A と B のどちらにしますか？」と文章で聞くのは禁止。

- 二択・多択・方針の確認・PR を作るかどうか、すべてダイアログで出す。
- **次のステップ・残件・未完了の作業を提示するときもダイアログで出す。**
  「残っているのは A と B と C です」と本文に並べて相手に選ばせるのは、
  疑問符が無くても違反。何を選べばいいのか分からない状態を作らない。
- 推奨があれば先頭に置きラベルに「(推奨)」を付ける。
- **他者・外部イベント待ちで止まる場合もダイアログで出す。** 待つこと自体が選択であり、
  待つ以外の道（別手段・先に別作業・中止）を利用者が選べる状態にしておく。
- 本文だけで終わってよいのは、**残作業がゼロで、利用者が次に何も決めなくてよいとき**に限る。

#### 言い換えて回避するのは、選択肢を本文に並べるより悪い

フックに差し戻されたとき、**検知語を避けて書き直すのは違反である。**
差し戻しは「書き方を直せ」ではなく「ダイアログを出せ」という指示。

実際に起きた回避の例（すべて違反）。

- 「残件」「次のステップ」を消して「〜まで進みません」と言い換えた
- 「どうしますか？」を消して「連絡をもらえた時点で再開します」と言い換えた
- 判断を利用者に委ねたまま、疑問符だけ削って言い切りの形にした

いずれも**残作業がある状態でダイアログを出していない**点で同じ違反。
差し戻されたら、文面ではなく `AskUserQuestion` を出すことで応じる。

**このルールに例外は無い。** 文書に書くだけでは守られないことが実際に起きたため、
フックで強制している。仕組みは [`docs/dialog-rule-enforcement.md`](docs/dialog-rule-enforcement.md) を参照。

### 2. コストは必ず先に示す。API 課金が発生する操作は事前確認（例外なし）

**費用の話を後回しにしない。** 単発の実行も、毎日回り続ける反復ジョブも、
**いくらかかるのかを示してから**着手する。以下の 2-A と 2-B は同じルールの表と裏である。

#### 2-A. Console 経由で費用が発生しうる操作は、いかなる作業であっても着手前に利用者へ確認する

リサーチ・検証・動作確認・「ちょっと試すだけ」も対象。

**これは禁止ではない。** 費用がかかること自体は問題ない。防ぎたいのは
**利用者が知らないところ、認識が甘いところで費用が発生すること**。順序を守る。

1. **先に見積もる。** 使うモデル・想定トークン数・単価から概算を出す。
2. **`AskUserQuestion` で提示する。** 何のために、いくらかかる見込みかを示して許可を得る。
3. **許可を得てから実行する。** 見積もりを大きく超えそうなら、その時点で再度確認する。

- 対象: API キーを使う呼び出し全般（Anthropic / OpenAI / Google など）、
  `ANTHROPIC_API_KEY`・`CLAUDE_API_KEY`・`OPENAI_API_KEY` 等を用いるスクリプトやコマンド、
  課金エンドポイントへの直接リクエスト。
- **対象外**: Claude のサブスクリプション枠内で動くこの対話そのもの（Claude Code / Cowork）。
  これはプラン利用であり Console 課金ではない。
- 見積もりが立てられないなら、それ自体を伝えて判断を仰ぐ。黙って実行しない。

**このルールもフックで強制している。** `.claude/hooks/guard-api-cost.py` が該当コマンドを
ブロックする。仕組みは [`docs/api-cost-guard.md`](docs/api-cost-guard.md) を参照。

#### 2-B. 反復作業は、必ずコストとセットで示す（例外なし）

**定期実行・自動化・「これから毎日回すもの」を提案・報告・変更するときは、
その API クレジット消費量を必ず同じメッセージの中で示す。** 聞かれてから答えるのでは遅い。

反復作業は 1 回あたりが小さくても、**回数が積み上がって効いてくる。**
どれだけ使っているかを利用者が把握できない状態を作らないための規定である。

示す単位は次の 3 つを揃える。**1 回あたりだけ示して終わらせない。**

| 単位 | なぜ必要か |
| --- | --- |
| 1 回あたり | 単価。設計を比べるときの基準になる |
| 1 日あたり | 実行頻度を掛けた実額。上限の妥当性がここで分かる |
| 1 か月あたり | 判断に効く額。日額だけだと小さく見える |

- **単価は記憶で答えない。** `claude-api` スキルの料金表など、根拠のあるものから引く。
- **実測と推定を必ず区別する。** 推定なら前提（モデル・入出力トークン数）を明記する。
  前提が書けないなら「未確定」と言う。**それらしい数字を置かない。**
- **量を増やす提案は、増加後の月額を必ず併記する。** 「上限を 8 件から 32 件に上げる」は
  コスト 4 倍の提案であり、その事実を伏せて出さない。
- **上限の数字を、実績のように出さない。** 上限は「暴走してもここで止まる」安全弁であって、
  予想コストではない。両方を出すときはどちらがどちらかを明示する。
  実際に、実績の 10〜20 倍にあたる上限値を実績のように並べ、利用者に指摘された。
- 現在稼働中のジョブの実額は
  [`docs/recurring-job-costs.md`](docs/recurring-job-costs.md) に置く。**変更したら更新する。**

**このルールも毎ターン注入している。** `.claude/hooks/dialog-rule-reminder.sh` が
ルール 1 と一緒にこの規定をコンテキストへ入れ直すため、会話が長引いても薄れない。

### 3. 会話の中だけに状況を残さない

セッションの実行環境（コンテナやブリッジ接続）は予告なく消える。そうなると会話からは
状況を引き出せなくなる。Git にコミットされたファイルだけが確実に残る。

- 決まったルール・方針は会話ではなくこの `CLAUDE.md` に書く。
- 進行中の状況は [`docs/session-handoff.md`](docs/session-handoff.md) に書く。

### 4. X への投稿は OpenClaw が行う

Claude が X に直接投稿することはない。確立している経路は次のとおりで、勝手に省略しない。

1. `node scripts/generate-tweet.mjs <slug>` で草案を生成する。
2. 草案を Slack で Jordan（`<@U0A5V22PVTQ>`）に送る。
3. **👍 が付くのを待つ。** 確認前に先へ進めない。
4. 👍 を得たら OpenClaw が投稿する（`@OpenClaw tweet <slug>`）。

### 5. 自分の役割の外を勝手に触らない（例外なし）

セッションは役割で分かれている。**担当外の領域は、直せると分かっていても自分で直さない。**

| セッション | 担当 |
| --- | --- |
| `daily-hack-blog3` | **ブログ記事**の企画・執筆・画像・公開 |
| `daily-hack-tweet2` | **X の運用・戦略・自動化** |

- 迷ったら、成果物が「記事」なら blog3、「X 上の動き」なら tweet2。
- **担当外のことに気づいたら、直さずに
  [`docs/cross-session-requests.md`](docs/cross-session-requests.md) に依頼として書く。**
  黙って見送るのも、勝手に直すのも、どちらも違反。
- 2 つのセッションは**直接会話できない**。届くのは Git にコミットされたファイルだけ。
- 共有ファイル（`CLAUDE.md` / `docs/session-*.md` / `.claude/hooks/`）を触る前に必ず `git pull`。

**2026-08-16 に、blog3 が X 運用の設計書を書き換え、同時刻に tweet2 も同じファイルを
更新していてマージ衝突が 2 回起きた。** 境界と受け渡しの詳細は
[`docs/session-roles.md`](docs/session-roles.md) を参照。

### 6. 記事を書く前に `blog-article` スキルを読む（例外なし）

**記事の新規作成・加筆・リライト・画像の用意は、`blog-article` スキルを読んでから着手する。**
本文を 1 行でも書く前に読む。

```text
Skill(skill="blog-article")   ← 実体は .claude/skills/blog-article/SKILL.md
```

**2026-08-22 に、読まずに書いた記事がサウナと無関係の表紙で公開され差し戻された。**
`eyecatchUrl` を書かないとカテゴリ共通のフォールバック画像が表紙になるが、
**ビルドは通り、警告も出ない。** だから気づけない。

既存のパイプライン（`scripts/gen-*-eyecatch.py` / `fetch-commons-photo.py` /
`check-article-ux.py`）を使わずに書いたのが原因で、**同じことを繰り返さないために
スキルをリポジトリにコミットしてある。**

### 7. 返信の文面を触る前に `x-reply-style` スキルを読む（例外なし）

**X の返信文を書く・テンプレートを直す・返信量を変えるときは、
`x-reply-style` スキルを読んでから着手する。** 1 行でも書く前・1 つでも設定を変える前。

```text
Skill(skill="x-reply-style")   ← 実体は .claude/skills/x-reply-style/SKILL.md
```

**2026-08-27 に「いまけっこうヒヤヒヤするコメントしている」と指摘された。**
アカウント名の「あんたバカぁ？」は自分の TL の芸だが、**他人の投稿への返信に
持ち込むと絡みになる。** 相手の投稿が損・失敗・トラブルの報告でも同じ文面が当たる。

売春系を弾く `ng-filter` は**入口**（相手を弾く）だけで、
**こちらが出す文は無検査で X に出ている。** その穴と直し方をスキルに書いてある。

**フィードバックは会話に残さない。** 指摘を受けたら
`.claude/skills/x-reply-style/SKILL.md` §6 の表に追記する。書かないと次で消える。

## OpenClaw 連携

OpenClaw は利用者の Mac（`home-mac` / 192.168.2.102）で動く常駐エージェント。
クラウドセッションからは到達できないため、OpenClaw 側の作業は依頼する形になる。

| 項目 | 内容 |
| --- | --- |
| Slack チャンネル | **2 つある。用途が違う**（下表） |
| Jordan の Slack ID | `U0A5V22PVTQ`（**fieldbeside** ワークスペース） |
| Slack 投稿トークン | `~/openclaw/config/.env` の `OPENCLAW_BOT_TOKEN` |
| 記事公開 | `scripts/blog-publish.sh <slug> "<title>"` を OpenClaw が呼ぶ（ビルド検証 → PR 作成） |
| 定期ジョブ | `ai.openclaw.sitemap-autosubmit` / `ai.openclaw.seo-health`（launchd） |

### 機械的な操作は `ops/tasks/` に置く。セッションに依頼しない

**launchd ジョブのロード・ファイルの確認・設定の変更のような「決まった手順」は、
Mac のセッションに頼まない。** `ops/tasks/*.sh` にコミットすれば、30 分ごとに走る
`ops-heartbeat.sh` が実行し、結果を `heartbeat.json` の `tasks` に書いて push する。

```text
ops/tasks/001-load-follower-snapshot.sh をコミット
  → 30 分以内に Mac で実行される
  → 結果が heartbeat.json に載る（rc と出力の先頭 300 字）
  → 実行済みの印が残り、二度は走らない
```

**この経路は接続・認証・会話 ID のどれにも依存しない。** セッション経由の依頼は
その 3 つすべてに依存し、2026-08-16〜19 に 3 つとも壊れて**依頼が 3 日間 1 件も
届かなかった。** 同じことを繰り返さないための経路である。

- **出力は公開リポジトリに載る。** タスク側で秘密を出力しない
- **当て推量でファイルを作らない。** 見つからなければ候補を挙げて `exit 1` する
- 対話や判断が要る依頼だけ、下のトリガー経路を使う

### 対話が要る依頼はトリガー経路。Slack に書いた依頼は届かない

**Slack は報告を受け取る場所であって、依頼を出す場所ではない。**

`C0A5FKU7T5M` に「OpenClaw:」名義で返ってきていた長文の作業報告は、
**Mac の Claude セッションがボットトークンで投稿していたもの**で、
`create_trigger` / `fire_trigger` で送った依頼への応答だった。
`C0B4CJHH797` の稼働報告は launchd のジョブが定期的に出しているだけで、
**Slack に書いた依頼を読む主体は確認できていない。**

```text
依頼: create_trigger（persistent_session_id = Mac セッション）→ fire_trigger
報告: Slack（両チャンネル）／ PR ／ ops/heartbeat のいずれか
```

トリガーには落とし穴が 2 つある。

- **毎回新しく作る。** 使い回して `fire_trigger` すると別セッションに紐づくことがある。
  応答の `session_id` の末尾が対象セッション ID と一致していることを必ず確認する
- **コネクタは乗らない。** 起動されたターンに `mcp__Slack__*` が無いため、
  Slack へ出させるなら `OPENCLAW_BOT_TOKEN` を使った `curl` を依頼文に書く

### Slack チャンネルは 2 つある。片方だけ見て「沈黙」と判断しない

| チャンネル | ID | 流れているもの |
| --- | --- | --- |
| `#fun_reward-hack_tweet` | `C0A5FKU7T5M` | X 投稿の草案・承認（👍）・投稿結果 |
| `#fun_reward-hack_blog` | `C0B4CJHH797` | **OpenClaw の稼働報告・異常検知・復旧通知** |

2026-08-16 に、`C0A5FKU7T5M` だけを見て **OpenClaw が 10 時間沈黙していると誤認した。**
実際には `C0B4CJHH797` へ稼働報告が届き続けていた。

```text
✅ OpenClaw 復旧  2026-08-16 17:46:31
• Listener process: running (pid=850)
• Listener log: listener.log fresh (2.8min ago)
```

**「応答が無い」と判断する前に、両方を見る。** 依頼を出すときも、OpenClaw の稼働に
関わるものは `C0B4CJHH797` に送る。

`main` は保護ブランチで直接 push できない。OpenClaw も Claude も必ず PR 経由で入れる。

OpenClaw が応答しない・虚偽の完了報告をする場合の復旧手順は
[`docs/openclaw-recovery.md`](docs/openclaw-recovery.md) を参照。

### 緊急依頼は 1 件だけ送る

**削除・停止・取り消しのような 1 操作の緊急依頼を、他の依頼と一緒に送らない。**
2026-08-15 に、ボタン 1 つで済む投稿削除が 5 件の依頼の後ろに埋もれ、
2 時間半 実行されず、最終的に利用者が手で消した。

- 緊急依頼を送ったら、**返答が来るまで他の依頼を送らない**
- **15 分で応答が無ければ、再送ではなく利用者に伝える。** 黙って待たない
- **1 操作で済むことは、投げる前に「自分でやるか依頼するか」を選べるようにする。**
  利用者の手が最速であることは珍しくない

詳細は [`docs/urgent-request-protocol.md`](docs/urgent-request-protocol.md) を参照。

**クラウドセッションから Mac へは SSH で届かない**（ssh 未インストール・鍵なし・生 TCP が
出られない。2026-08-11 に実測）。OpenClaw を触るときは Mac で `claude` を起動して
`/remote-control` で公開する。手順は [`docs/session-recovery.md`](docs/session-recovery.md) を参照。

## Slack の操作は許可済み

2026-08-11 に利用者から Slack 操作の全面的な許可を得ている。**毎回の許可確認は不要。**
`.claude/settings.json` の `permissions.allow` に `mcp__Slack__*` を登録済みで、
メッセージ送信・スレッド返信・リアクション・読み取りはそのまま実行してよい。

ただし次の 2 点は許可の対象外であり、従来どおり守る。

- **X への投稿**は Claude が行わない（最上位ルール 4）。
- **👍 を得る前に投稿へ進めない。** Slack に草案を出すところまでが Claude の範囲。

## Slack の接続先を最初に確かめる

Slack を使う作業の前に、コネクタがどのワークスペースに繋がっているか必ず確認する。

```bash
# slack_read_user_profile を引数なしで呼ぶ
```

- **正**: `U0A5V22PVTQ` / fieldbeside ワークスペース。
- **誤**: `naoki@taxatech.com` / Organization Name が `Taxa`。この状態では `C0A5FKU7T5M` が
  `channel_not_found` になり、fieldbeside 側は一切読めない。利用者に再接続を依頼する。

## セッションの引き継ぎ

- 現在の状況・進行中の作業・次のアクションは [`docs/session-handoff.md`](docs/session-handoff.md) に書く。
- 作業を終える前に記録を追加する。

  ```bash
  npm run handoff -- "やったこと" --next "次にやること"
  ```

- 新しいセッションでは `.claude/hooks/session-start.sh` が上記メモと Git の状態を
  自動で読み込むため、利用者が状況を説明し直す必要はない。
- セッションが操作不能になったときの復旧手順は
  [`docs/session-recovery.md`](docs/session-recovery.md) を参照する。

## Slack への草案投稿フォーマット

X 投稿の草案を Slack に出すときは、OpenClaw が使っている形式に必ず揃える。
親メッセージ＋スレッド返信で構成する。

### 親メッセージ

```text
<@U0A5V22PVTQ> 📝 ブログ告知スレッド ドラフト（承認待ち）
記事: <記事タイトル>
<記事URL>
形式: N連スレッド ／ 各投稿に画像つき
承認をもらえたら投稿します。
```

### スレッド返信（投稿ごとに1件）

```text
[n/N] 🖼️ <画像ファイル名>
（ツイート本文をコードブロックで囲む）
```

- 画像は Slack コネクタから添付できないため、別途 OpenClaw にアップロードを依頼する。
- 依頼にはリポジトリ・ブランチ・コミット・ファイルパスを明記する。
- **承認（👍）前に X へ投稿しないことを毎回明示する。**
