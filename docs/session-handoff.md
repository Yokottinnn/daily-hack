# セッション引き継ぎメモ

> **このファイルは Git にコミットされる。** セッションや実行環境（コンテナ／ブリッジ）が
> 消えても内容は残るため、新しいセッションはここから状況を復元できる。
>
> 新セッション開始時に `.claude/hooks/session-start.sh` が自動でこの内容を読み込む。
> 作業を終える前に `npm run handoff -- "やったこと" --next "次にやること"` で記録を追加すること。

## 現在の状態

- 本番ブログ（Astro）は `main` から Cloudflare Pages にデプロイされる。
- 直近のリリースは `#168`（コスト先出しルールの最上位ルール化、認証失効/緊急依頼の埋没検知）まで完了。
- ららぽーとガイド 2026 の X 告知は投稿済み（8/11、画像4枚＋リプライ2連投）。

## 進行中の作業

**15分クイックリプライ施策（依頼4）が DRY_RUN 中に OAuth 失効で中断。** 実投稿・有効化はまだしていない。

- `~/.openclaw/workspace/scripts/quick-reply-watcher.js` はコード完成・plist 未作成＝**未ロード（安全）**。
- 対象アカウント8件（`data/quick-reply-targets.json`）はフォロワー2,290〜4,512・節約/ポイ活/家計ジャンルで選定済み。
- 8/15 の DRY_RUN 実行では、検知（Playwright DOM 取得・LLM不使用）と15分判定は正常動作を確認したが、
  対象8件すべて投稿が15分を大幅超過（数万〜数百万分＝固定ツイート等）で「見送り」となり、
  **生成（`asuka-fill.js` 呼び出し）パスは一度も通っていない＝未検証**。

### 8/15 に起きた事故と対応（重要）

1. `poll-approvals` が5日間停止（Chrome自動更新起因）→ 再開直後に承認から8日経過した滞留エントリを
   処理してタイムラインへ意図しない投稿（`2088486949527196002`）。
2. Jordan が手動で削除。**現在 404 で削除済みを確認済み**（8/16 再確認）。
3. 再発防止として `poll-approvals.js` と `queue-manager.js list-awaiting`（共通口）の両方に
   **承認TTL 7日**を実装。実戦投入時に27件の滞留を「投稿せず失効」させ、誤爆ゼロを確認。
4. `comment-warmup` が実機で `MAX_PICKS_PER_FIRE=8`（想定2の4倍、32件/日）で動いていたことも発覚。
   Jordan 判断で **2 picks（8件/日）に戻し済み**（現在の plist で確認済み）。
5. 外部監視 `com.dailyhack.ops-heartbeat`（30分毎に `ops/heartbeat` ブランチへ push）を launchd 登録し、
   GitHub Actions 側（`ops-watchdog.yml`）で死活監視できる状態にした。

## 次のアクション

- [ ] 15分クイックリプライ: DRY_RUN で生成パスを検証するか判断（対象アカウントが実際に15分以内投稿
      するタイミング待ちか、能動的に再実行するか）。**費用は1件あたり実測 $0.00417（Haiku 4.5）、
      日次cap5なので最大 $0.0209/回**。
- [ ] 生成パスが検証できたら、`poll-approvals`（TTL実装済み・現在停止のまま）を戻すかを判断。
- [ ] `poll-approvals` を戻す際は TTL 実装のテスト（25時間前の偽エントリで投稿されないこと）を先に行う。

## 決定事項・注意点

- **セッションが「もう会話できません」になった場合**、会話履歴は失われていない。
  原因はセッション本体ではなく実行環境の消滅であり、復旧手順は
  [`docs/session-recovery.md`](./session-recovery.md) にまとめてある。

- **クラウドセッションから Mac へは SSH で届かない。** ssh 未インストール・鍵なし・
  生 TCP が出られない（2026-08-11 に実測）。OpenClaw を触るときは Mac で `claude` を
  起動して `/remote-control` で公開する。

- **リポジトリのパスは `/Users/ny/projects/anta-baka-x/blog`。** ディレクトリ名が
  `daily-hack` ではないため、名前で検索しても見つからない。

- **OpenClaw の MUST rule の実体は `~/.claude/projects/-Users-ny--openclaw-workspace/memory/`。**
  `~/.openclaw/workspace/memory/` ではない。場所を間違えると、有るのに「無い」とも、
  無いのに「有る」とも報告しうる。詳細は
  [`docs/openclaw-recovery.md`](./openclaw-recovery.md) を参照。

- **ログが動いても「ジョブが復活した」証拠にならない。** listener が生きているため、
  未ロードのまま同じログに書き込まれることがある。判定は `launchctl list` で行う。

<!-- session-log:start -->
## セッション記録

<!-- 新しい記録がこの下に追加される（新しいものが上） -->

### 2026-09-20 — フォロワー経路の効き目を測り切った。実フォロワーは 264（227 ではない）。競合の種ごとの返り率は himawari56757 26.0% 〜 ukk_hx 4.2% で 6 倍 の開き。群で見ると 外す 3 つ が 89 件→7 人（7.9%）、残す 4 つ が 133 件→28 人（21.1%）。規模では説明がつかず（okamiler_pn 18.2%/平均7757 と ukk_hx 4.2%/平均6993）、効いているのはジャンルの近さ。種の一覧は competitor-follower-follow.js:27 の COMPETITORS に直書き、epoch日%7 で 1 日 1 種。環境変数では渡せない。influencers.json は 7 件 のみで候補プールは空。候補は hashtag-follow の「大きすぎる」弾き（711SEJ 707万〜Gian_Support 1.1万）と comment-warmup の選定頻度（money_yossy 85回, POIKATSU_OTAKE 43回, fxmeitantei 30回, bicsim_official 22回, coupon_gorilla1 11回 など）から拾える

次のアクション:

- [ ] 候補アカウントの実際のフォロワー数とプロフィールを測ってから 3 つ を選ぶ。大手ブランド公式はフォロワーが一般層なので種には向かない可能性が高い。決まったら competitor-follower-follow.js:27-31 の配列を完全一致で置き換える（/bin/bash）

### 2026-09-20 — heartbeat.json が 9/18 以降 不正な JSON で監視が 2 日間 素通りしていたのを発見・修正（#518）。x91 で未ロード 60 件 を仕分け、ゴミではなく 2026-09-09 の tab-guard 一斉 unload の生き残り 48 本 だと確定。x92 で follower-snapshot を戻し、実フォロワーが 227 ではなく 264 だったことが判明（+37 / 12 日）。フォロワー記録の停止を heartbeat の record_stale として鳴らすようにした（#521 #522）

次のアクション:

- [ ] LLM を呼ぶ 48 本（draft-* / engage-daily / qt-* / trend-daily / grok-trending-daily など）を戻すかどうか、1 本ずつコストを出して判断する。grok_trending の「healed と言い続けて直らない」ループと、comment-warmup の終了コード 5（auto-reply.js:176）も未確定

### 2026-09-20 — tab-guard の 2 バグ修正（x89）が当たったことをレポートで確認（PID=26232・終了コード 0・30 秒 でエラーログ増加 0・状態ファイルに reachable/chrome_alive_at）。さらに heartbeat.json が 9/18 以降 不正な JSON で、ops-watchdog の jq が全部 落ちて監視が 2 日間 素通りしていたのを発見し修正（#518 マージ済み a2eb686）。生成側で自己 parse 検査、監視側で jq -e . の前置きと badjson 警報を追加。CLAUDE.md 最上位ルール 13 に grep -c || echo 0 の落とし穴を追記。終了コード 5/2 を測る x90 を投入

次のアクション:

- [ ] x90 のレポート（reports/exit-5-and-2.md）を読み、comment-warmup の 5 が「候補なし」かどうか、pipeline-heartbeat のどの check が CRIT かを確定する。次の heartbeat で heartbeat.json が正しい JSON になっているかも確認する

### 2026-09-20 — 再起動でジョブが外れる原因が確定。tab-guard.js:69 の haltAutomation が ai.openclaw.*.plist を tab-guard 以外すべて unload する。発火条件のひとつが「Chrome プロセスの消滅」で、再起動すると必ず成立するため 9/12 と 9/20 の両方で全滅した。com.dailyhack.* はパターン外なので無傷だった。さらに tab-guard が 10 秒 ごとに再起動を繰り返していてログが 20MB。comment-warmup の終了コード 5 は返信欄が見つからない DOM エラー（ただし 01:17 には投稿成功しているので全滅ではない）

次のアクション:

- [ ] tab-guard の「Chrome 消滅」判定が起動直後に誤発火しないようにする。起動時に載せ直す仕組みは tab-guard の停止と綱引きになるので入れない。tab-guard の再起動ループと comment-warmup の textarea 未検出も別途

### 2026-09-20 — 学割記事の一覧をカテゴリ別に増やした（PR #509）。都度を映画館・テーマパーク・本カラオケ引越し託児・買い物の4つに分割。t099 で放送大学のサイトを辿り、共済加入者の入学料半額（全科24,000→12,000円・被扶養者も対象・共済専用出願票が必須）、託児所提携、ウチダのOffice学割を発見して追加。10年の最安が42,000円＝月350円になる。日経電子版は「23歳以下」を公式で確認できなかったため年齢制限の表から外した。

次のアクション:

- [ ] X 告知を tweet2 に依頼するかを利用者に確認する。未インデックス9記事の Search Console 申請も残っている

### 2026-09-20 — 放送大学 学割の記事を下書きフィードバック6件で作り直した。在籍を10年で見る構成（全科履修生・除籍は4学期連続の未納・最安54,000円＝月450円、休学込み14年で月321円）、サブスクと都度の分離、ac.jpメール無料枠と国立美術館キャンパスメンバーズ（全学生種OK）の新設、年齢で弾かれる学割の節、定期券は学割証で買えない事実の明記、表紙を6分割タイル（Commons実写6枚）に差し替え。アーティファクトも10年ベースに更新。

次のアクション:

- [ ] PR #499 のプレビューでレビューをもらう。マージは利用者の判断待ち

### 2026-09-20 — 2026-09-19 11:21 JST に Mac が落ち、9/20 00:20 JST に復帰。ops の経路（ポーラー・heartbeat・タスク実行）は正常に戻り t092〜t095 が rc=0 で完了。ただし LaunchAgents が載り直しておらず x_jobs loaded 0/8、last_reply は 19 時間 前で stale。認証と CDP は生きている。番人も外れているため自動では戻らないので、x84 で 12 本 を bootstrap する

次のアクション:

- [ ] x84 の結果を launchctl list で確かめる。載った後は次の定時（comment-warmup 12/16/19/22 時 ほか）で自然に戻る。いま走らせるなら kickstart だが LLM 代が出るので承認を取ってから

### 2026-09-19 — 参照元の X 投稿（connect24h/2100750468171657241）を取る t093 を main に入れた（PR #500）。クラウドは x.com・cdn.syndication.twimg.com・publish.twitter.com・api.fxtwitter.com・api.vxtwitter.com すべて EGRESS_BLOCKED で読めない。ops-heartbeat が 2026-09-19 11:21 JST から停止中のため、t093 も t092 も 1 度も実行されていない。

次のアクション:

- [ ] 投稿本文が手に入り次第、放送大学 学割の記事（PR #499 / プレビュー https://273878df.daily-hack.pages.dev/posts/hoso-daigaku-gakuwari-2026/）に取り込む

### 2026-09-18 — 歩いてポイ活のレビュー3件に対応: 表紙を実アプリ6枚のタイルに作り直し、20アプリ全てにApp Store公式スクショと仕様表を追加、記事の「21」を実態の20に訂正

次のアクション:

- [ ] プレビューで再レビューをもらう。東京健康UPlusはアプリ無し（都のWebサービス）として記載

### 2026-09-18 — 週次レポートを kickstart で即時検証し、Slack への到達と ${ALL_VISITS} の消失を実物で確認（2026-09-18 21:41）。警報を水準ベースに変更、クローンの遅れを heartbeat に常時表示、空セクションの明示化まで完了

次のアクション:

- [ ] 2026-09-21（月）08:00 の定時レポートが自動で届くことを確認。GSC画面から未登録9本のインデックス登録をリクエスト

### 2026-09-18 — 認証が 2026-09-18 20:42 JST に失効（CDP も不健全）。ジョブは 8/8 載っていて番人も 9/9。今日の実投稿は 10 件（x_tweet_id 一次情報・heartbeat の posted_today 32 はログ行数）。今日のフォローは 6 件 で 6/6 フォロワー数を記録できている。帯ごとの返し率が全帯 0% だったのは集計側のバグで、reply-followback-check が書くのは followback_status なのに follows_back だけを見ていた。ops-heartbeat.sh の集計を両方 見るよう修正し、status_values も出すようにした

次のアクション:

- [ ] 次の heartbeat（30 分 以内）で followback_bands の率が出るか確認する。認証は過去に自動復旧した実績があるので、次の heartbeat で auth.ok が戻るかも見る

### 2026-09-15 — 「なぜ初歩的なミスが起きるのか」を仕組みまで遡って調査。4つの失敗（CI未接続・lastmod欠落・差分アラーム・レポートの構造的盲点）を特定し、品質チェックのCI組み込み（ベースライン方式）とMacクローンのmain復帰＋週次レポート実体の入れ替えまで完了

次のアクション:

- [ ] 次の月曜08:00の週次レポートがSlackに届き、未展開変数が無いことを確認する。GSC画面から未登録9本のインデックス登録をリクエスト

### 2026-09-15 — x81 で mutual-prune の反映漏れを解消（ずれ 2 件 → 0 件、本体にも反映処理を追加）。x82 で滞留の実数を確認したところ 58 件 ではなく 4 件 で、全部 相互フォロー。cleanup は毎時・上限 20 件/回 で既に流し切っていた。レポートにハンドルが裸で漏れたため、ops/lib/task-preflight.sh の hide() をカンマ区切りの列にも対応させた

次のアクション:

- [ ] 滞留の残り 4 件 は全部 相互なので、外すか残すかの方針を決める。X 告知 3 本（ドローン・格安スーパー・歩いてポイ活）は保留中

### 2026-09-15 — 大磯プリンス記事の PV/検索流入を実測し、インデックス未登録が原因と特定。記事をてこ入れ（X実投稿2件・YouTube2本・写真カード6枚・内部リンク3本）し、サイトマップに lastmod を追加。全74本のインデックス状況も実測

次のアクション:

- [ ] GSC画面から未登録9本のインデックス登録をリクエスト（1日10〜12本上限）。数日後に t076 系で coverageState を再確認

### 2026-09-15 — mutual-prune の実績を確認（x78/x79）。9/13 に 6 件 外しており、全部 休眠 31〜71 日。記録は専用ファイル mutual-prune-state.json に在り、ログと件数が一致。x78 が reply-followers.json を見て「0 件」と書いたのは私の確認ミス。ただし 20 時間 走っていない時間があり、原因は StartInterval が Mac の睡眠中の発火を落とすこと。x80 で StartCalendarInterval（6時/18時）に変換

次のアクション:

- [ ] x80 の結果で載ったかを launchctl list で確かめる。番人は既に mutual-prune を監視対象にしているので追加不要。X 告知 3 本（ドローン・格安スーパー・歩いてポイ活）は利用者の判断で保留中

### 2026-09-15 — 承認待ちキューの掃除を完了（x76 で 18 件 ＋ x77 で 1 件 ＝ 19 件 に印、TTL 内の trend 4 件 は保持）。followback_bands が稼働して 28 件 記録済み（全部 72 時間 未満なので率はまだ null）。利用者の判断で X 告知 3 本 は保留。mutual-prune が走って何を外したかを確かめる x78 を投入

次のアクション:

- [ ] x78 の結果で mutual-prune が動いているかを確定する。0 件 なら「壊れている」のか「条件に当たる相手が居ない」のかを切り分けてから条件を触る

### 2026-09-15 — 承認待ちのまま止まっていたキューを掃除（x76 で 18 件 / x77 で残り 1 件）。TTL 内の trend 4 件 は残した。x76 は対象 19 件 に対し 18 件 しか打っておらず、原因は join("\n") で末尾に改行が無く while read が最後の 1 行を読み飛ばしたこと。rc は全部 0 で気づけない形だったので CLAUDE.md ルール 14 に追記

次のアクション:

- [ ] お台場ドローンショー・都心の格安スーパー・歩いてポイ活（9/13 に blog3 が追加）の 3 本 の X 告知が未着手。格安スーパーは Mac 側に画像が無いので pull 状況の確認から

### 2026-09-14 — 記事の報告にプレビュー URL を必ず添える規定を CLAUDE.md 最上位ルール16 と blog-article スキルに記録（PR #465 マージ済み）

次のアクション:

- [ ] 歩いてポイ活の記事レビュー待ち。X 告知依頼は tweet2 に未対応が4件

### 2026-09-14 — 「歩いてポイ活 2026年最新版」を公開（PR #463）。21アプリを5カテゴリの勢力図に。1日1万歩×1年を統一換算し、無料・確実の最大は RenoBody 年365円。X実投稿6件・YouTube5本を検証して埋め込み。pos-axis の white-space バグも修正

次のアクション:

- [ ] X 告知は tweet2 へ依頼済み。記事の再レビューがあれば対応

### 2026-09-14 — x75 で 2 件 確定。① 8/30 に出た 2094061373584208194 はサウナ新店記事の告知で、sauna-x-20260830-223943 は同じ投稿の重複。未出は お台場ドローンショー のほう。② 承認待ちのまま残る 23 件 は poll-approvals が停止中のため失効処理が走っていないだけで、queue-manager の TTL ガードが投稿対象から除外しているので投稿される危険は無い

次のアクション:

- [ ] お台場ドローンショーと都心の格安スーパーの告知を作る。格安スーパーは Mac 側に画像ディレクトリが無いので、リポジトリの pull 状況を先に確認する

### 2026-09-14 — x74 でキューの告知を全部 洗い出した（一次情報＝x_tweet_id）。IKEA豊洲 2096512177930903788 とモーニング 2096230281909006590 は 9/06 に出ていた。サウナは 1 本 出て（2094061373584208194）もう 1 本 は skipped_unspecified で止まったまま。都心の格安スーパーはキューに 1 件も無く本当に未着手。依頼一覧に根拠を書き込んだ

次のアクション:

- [ ] 都心の格安スーパー（9/06）の告知を作る。サウナの残り 1 本 は、どちらが未出かを記事と投稿内容で突き合わせてから判断

### 2026-09-14 — IKEA豊洲の X 告知は 2026-09-06 に投稿済みだったことを利用者から指摘され、依頼一覧を完了に直した（https://x.com/heng_ji31590/status/2096512177930903788）。同じ表に残っている対応中・未対応をキューの x_tweet_id で突き合わせる x74 を投入

次のアクション:

- [ ] x74 の結果でサウナ 2 記事（8/27）と格安スーパー（9/06）が出ているかを確定し、出ていないものだけ告知を作る

### 2026-09-13 — 帯ごとのフォロー返し率を heartbeat.json に出す仕組みを追加（followback_bands）。新しい常駐ジョブを足さず、30 分ごとの ops-heartbeat に 1 項目 足す形。mature=フォローから 72 時間 以上 経った分だけで率を出し、件数が 20 未満のときは「判断しない」と明示する

次のアクション:

- [ ] 数週間 溜まったら followback_bands を見て、follower count の上限 50000・下限 100 の妥当性を判断する。止まっている X 告知（格安スーパー 9/06・IKEA 9/05）と滞留 58 件 のアンフォローは未着手

### 2026-09-13 — x72/x73 を実行。exec err 28 件 は 8/4〜8/7 に集中した過去の事象で、9 月は 1 件だけと判明（前の報告を訂正）。返信は直近 16 回 すべて picked 4/4 で、律速は候補ではなく MAX_PICKS=4。hashtag-follow は返信ループと同じ供給元のため、先に取られた残りだけを見ている。MAX_PICKS は 4 のまま据え置くと利用者が判断

次のアクション:

- [ ] 数日 回して、x68 適用後の実際の投稿数とフォロー返し率（followers_at_follow が溜まる）を見てから枠の判断をやり直す。止まっている X 告知（格安スーパー 9/06・IKEA 9/05）と滞留 58 件 のアンフォローは未着手

### 2026-09-13 — x68（広告を選ぶ前に弾く）と x67（返信きっかけのフォローにフォロワー数を記録）を適用し、21:06 の手動起動で両方 効いたことを実測で確認。ad_skipped=3/considered=11/picked=4、followers=1100 の記録が初めて入った。今日の投稿は 12 件 すべて x_tweet_id あり。x69 で hashtag-follow が trend-detect を供給元にしていることが判明し、弾かれる理由は follower count out of range が 94 件 で突出

次のアクション:

- [ ] follow-handle.js の exec err 28 件 の原因を見る。フォロワー数の上限 50000・下限 100 は followers_at_follow が溜まるまで触らない。滞留 58 件 のアンフォローは未着手

### 2026-09-13 — x65/x66/x67 を実行。フォローの上限の実値を確認（competitor 30・hashtag 90・reply_follow 30・MAX_PICKS 4・FORCE_RUN=1 で日曜スキップ無効）。今日のフォロー 9〜11 件 のうち 9 件 が既に返信きっかけで、競合刈り取りは 30 件 試して 1 件 通過のみ。comment-orchestrator.sh にも followers_at_follow の記録を追加（bash -n OK・退避あり）

次のアクション:

- [ ] 候補を選ぶ前に広告を弾く変更（生成 9 → 16 件/日・月 $0.81 → $1.44・+$0.63）の可否を確認してから実装する。記録が入ったかは 22:00 JST の周回後に reply-followers.json で確かめる

### 2026-09-13 — x62/x63/x64 を実行。通過率は 56%（picked 16 / enqueue 9・9/13 実測）で、却下の主因はゲートではなく入口の広告投稿スキップ（$0）。source 別フォロー返し率は competitor-follower 11.9% / comment-orchestrator 21.5% / hashtag-follow 15.4%（全体 16.2%）。follow-handle.js が返す profile からフォロワー数を記録するよう competitor-follower-follow.js と hashtag-follow.js を修正（適用済み・退避あり）

次のアクション:

- [ ] 返し率の低い competitor-follower の比重を下げ、返信きっかけのフォローを増やすかを判断する。返信本数を増やす場合は LLM 代が増えるので、実測 1 件 $0.003 を基に増加後の月額を先に示す

### 2026-09-13 — アンフォロー修理の検証が 3/3 で通った（x60）。x61（フォロー返し・いまフォロー中かの記録）を main にマージ。x62 として通過率の実績とフォロワー数の書き場所を測るタスクを作成

次のアクション:

- [ ] x62 のレポートを見て、①通すときにフォロワー数を state へ書く ②却下理由の上位から通過率を上げる、の 2 本を別タスクで実装する（ルール 15・測ると直すを分ける）

### 2026-09-13 — 返信の真因を特定: trend-detect が {"ok":true,"count":0} で候補 0 件。返信自体は動いており累計 859 件・最後 09-13 03:06 JST。ジョブは 8/8 本、番人の 05:00 定時実行も動作（attempts:1 が 3 件）。tab-guard が login ロックを書いていた件は解決済み。最上位ルール 14（環境差）・15（1タスク5分）を追加

次のアクション:

- [ ] x54 のレポート reports/why-zero-candidates.md を読む。見るのは ①検索ページを開けてヒット0か ②DOMが読めない(scraped 0)か ③除外で全滅か。あわせて x55 の reports/candidate-sources.md で ①自分へのリプライ ②フォロー中TL ③ハッシュタグ検索 の何件取れるかを見て、取れた経路だけを次のタスクで配線する

### 2026-09-13 — tab-guard が login ロックを書いていたことを特定（ロックの中身が 'tab-guard'）。x37〜x40 を投入し PR #426/#428/#429 をマージ。3 ループは 4/8、返信は 75 時間 出ていない（最後は 2026-09-09 22:03 JST）

次のアクション:

- [ ] x40 のレポート reports/post-now.md を読む。見るのは ①今回 出た返信の数（キューの x_tweet_id）②log show が出す『61 秒 周期でジョブを外している主』の名前。返信が 0 なら comment-orchestrator.sh の直接実行ログを読む

### 2026-09-13 — インデックスを実測で確認（t071）：記事5本すべて PASS / Submitted and indexed / ALLOWED。canonical も重複扱いなし。ページ側のシグナル（canonical・sitemap・構造化データ・内部リンク67ページ）も全て正常

次のアクション:

- [ ] 記事の再レビュー待ち。X ループの復旧は tweet2 担当

### 2026-09-12 — X の認証は復旧（ok=true・2026-09-12 20:43 UTC まで）。ただし X 運用のループが 8 本とも欠落・未ロード 68 件で、フォロワーは 227 のまま動いていない。ops-watchdog は正しく鳴っている

次のアクション:

- [ ] X ループの復旧は tweet2 の担当（PR #415 と同根）。記事側は #414 をマージ済みで再レビュー待ち

### 2026-09-10 — t070 の結果が出た：各社のチラシ8ページを取得しても価格は1件もHTMLに無い（値段はチラシ画像の中）。実測として記事に追記。Mac の ops は 2026-09-10 11:48 UTC に復旧を確認

次のアクション:

- [ ] 記事の再レビュー待ち。X の認証トークンが失効中（tweet2 へ記録済み）

### 2026-09-08 — 格安スーパー記事のレビュー2巡目5件をマージ（PR #411）。Mac の ops は 22 時間 停止のままで t070 は未実行

次のアクション:

- [ ] 再レビュー待ち。Mac が戻ったら t070 の結果で同一商品の表を足す

### 2026-09-08 — 格安スーパー記事のレビュー2巡目5件に対応してマージ（PR #411）。買収前の店舗数・「同じ商品を横並びできない理由」・ドンキのミスリード除去・表記統一・買収のIR出典

次のアクション:

- [ ] t070（同一商品の値段）の結果待ち。ただし Mac の ops が 8 時間 停止中

### 2026-09-08 — 格安スーパー記事のレビュー10件に対応してマージ（PR #406）。関連ブロックが .section-with-mascot の中に入るバグと、未配信の広告枠が空欄で場所を取るバグも直した（全73記事に効く）

次のアクション:

- [ ] レビューの再確認 ＋ X 告知（tweet2 へ依頼済み・未対応）

### 2026-09-06 — 格安スーパー記事を公開（PR #392）。埋め込み9件を実測で検証（t065/t066・投稿者の帰属も確定）。累計の数字も確定（t068）：計測開始2026-05-10、GSC全期間でクリック52・表示1,082・当たり記事15本

次のアクション:

- [ ] X 告知（tweet2 へ依頼済み・未対応）。公開URLの生存確認はクラウドからは不可

### 2026-09-06 — 3 系統を実測で診断し、返信とアンフォローを復旧した。【返信】原因は asuka-reply.cjs（全文生成器）が Mac に一度も入っていなかったこと。9/2 の差し替えが届いていなかった。x01 で部品 4 本を配置、x02 で comment-orchestrator.sh の 111 行を asuka-fill.js → asuka-reply.cjs に配線、x03 で試験生成（8 件中 4 件 通過・実費 $0.024・単価 $0.003）、x04 で comment-warmup を 8 件/日 で起動（launchctl list で rc=0 確認）。21:23 の実行で新生成器が呼ばれることを確認したが、候補 2 件が両方 PR 投稿と商品告知で skip、実投稿はまだ 0 件。【アンフォロー】滞留 197 件の原因は reply-followers-cleanup（実行役）が未ロードだったこと。plist は壊れていない。x06 で CLEANUP_MAX_PER_RUN=20 を挿入して起動（rc=0）。ただし手動実行に macOS に無い timeout を使い rc=127 で落ちたため、実際に外れた件数はまだ 0。x08 で timeout を外して再実行する（PR #390・GitHub API のレート上限でマージ待ち）。【フォロー】既に正常。今日 16 件（緩和前は 1 件/日）。【訂正】CDP 18800 を見るスクリプトは現存しない（130 箇所すべて 18810）。壊れた 6 本の plist は別系統で滞留の原因ではなかった。

次のアクション:

- [ ] PR #390 をマージして x08 を走らせ、アンフォローが実際に何件 外れたかを状態ファイルの前後で確認する（unfollowed 16 / 期限到来 197 が起点）。0 件なら 0 件と報告する。あわせて comment-warmup の次の定時実行で実際に X へ返信が出るかを確認する。残件: unfollow-daily / unfollow-evening は同名スクリプトが無く起動引数を復元できていない（実体の特定が要る）。follow-watchdog / revenge-unfollow は XML に復元済みだが未ロード（滞留が落ち着いてから起動する）。
### 2026-09-06 — 都心の格安スーパー7社の記事を書き上げた（決算から1店舗あたり年商を算出・CC写真11枚・検査2本通過・全17画面を目視確認）。feat/tokyo-discount-supermarket-2026 に push 済み

次のアクション:

- [ ] PR を作ってマージ ＋ t064（PR #391）をマージ ＋ X 告知をどうするか決める

### 2026-09-06 — 新レポートが実機で動いた（GSC 完全に取得・CF はトークン未設置で失敗、設計どおり理由つきで落ちた）。実測: クリック37のうち30がららぽーと1本＝81%。1ページ目に29記事いるのに表示730。当たり語はららぽーと系に集中し、表示は出ているのに順位が悪い語が複数ある

次のアクション:

- [ ] Jordan: Cloudflare の API トークン（Account Analytics: Read）を作って ~/.config/daily-hack/cf-token に置く。並行して「ららぽーと 大きさ/店舗数ランキング」など既に表示のある語を取りに行く記事を書く
### 2026-09-06 — モーニング記事の X 告知を投稿完了（[1/2] 2096230281909006590 画像4枚 ／ [2/2] 2096230358161482222）。原因は文面の重み 293 > 上限 280 だけで、Chrome も CDP も画像も正常だった。IKEA豊洲の告知を準備完了（画像4枚: まとめ→立地→キャンペーン→サメ、文面 [1/2]274 / [2/2]243）。帯に実写を薄く敷く cover.bgImage を生成側に追加し、これを既定として x-post-images スキル §1 に書いた。投稿本文の書き方を x-post-copy スキルとして新規作成（声の引き元 3 つと記事から取れた型の表）。

次のアクション:

- [ ] 18:30 JST のリマインダー（trig_012R5fa7FAUvW24N4iALNaM6）で PR #372 をマージして IKEA 告知を出す。main が進んでいたら先に rebase すること（blog3 が並行してマージしており、2 回 巻き戻しかけた）。出したあと reports/post-ikea-thread.md で thread_chain 2 本の tweet_id を確認する。同じ PR に t052（モーニングのキューを posted に直す・二重投稿の防止）も入っている。t046/t047 は読了済み。アンフォロー 6 本が落ちる原因は **plist の中身が XML ではなく JSON**（plutil -lint が Unexpected character { と言う・87〜126 B）で、launchctl enable では直らない。もう一つ、古いアンフォロー系スクリプトが **CDP 18800** を見ている（正しくは 18810）。返信は comment-warmup が未ロードで完全停止（$0/日）。t056 で 3 系統の今日の実数を取る。
### 2026-09-06 — 週次レポートを scripts/weekly-blog-report.py として作り直した（PV・訪問・流入経路・検索順位・前回比の9節／節ごとに独立して失敗し理由を書く／取れなければ rc=1）。収益化の勝ち筋を調査し docs/monetization-plan.md に記録（クリック型はAdSenseが天井・nendは2024/3終了・本命は成果報酬とLinkSwitch）

次のアクション:

- [ ] PR #375 をマージ → t052(既存実装を読む)・t053(新レポートを実機で1回実行)が Mac で走るのを待ち、結果を見て launchd の切り替えを決める

### 2026-09-06 — fieldbeside.com（親ドメイン）の実体が別リポジトリ Yokottinnn/fieldbeside-website（index.html 1枚の静的サイト）だと特定し、ads.txt を足す PR を出した（fieldbeside-website#1）

次のアクション:

- [ ] Jordan: 忍者ツールズ → AdMax → 広告枠一覧 →「ads.txt を取得」で出た行をチャットに貼る。こちらで public/ads.txt に入れて PR を出す

### 2026-09-06 — AdSense 申請の準備を点検。審査に必要なページは全部揃っていた（記事72本・privacy・contact・about・disclaimer・robots・sitemap・PR表記）。ads.txt がサブドメインだとルートから SUBDOMAIN= で参照しないと無視される件を発見し記録（PR #374）

次のアクション:

- [ ] Jordan: ① AdSense に申請 ② fieldbeside.com/ads.txt に SUBDOMAIN=daily-hack.fieldbeside.com を置く ③ 忍者AdMAX の管理画面で ads.txt の行を取得

### 2026-09-05 — AdSense 審査準備を点検し、プライバシーポリシー第9条が事実と違っていた（未配信の AdSense を『利用しています』と断言、稼働中の忍者AdMAX は記載なし）のを直した。ads.txt に忍者AdMAX の行が無いことも判明（PR #373）

次のアクション:

- [ ] 忍者ツールズの管理画面で ads.txt の行を取得して public/ads.txt に貼る（Jordan の作業・収益に直接効く）

### 2026-09-05 — 本文中の関連記事を最低2箇所にする自動差し込みを実装し #367 でマージ（72記事すべて達成を実測）。広告はスマホ下部固定を記事ページに追加（利用者判断で 2026-05-31 の削除を覆した）。忍者AdMAX の1ページ3個までと AdSense の状況を調べ docs/ad-slots.md に記録（PR #371）

次のアクション:

- [ ] Cloudflare Pages の環境変数に PUBLIC_ADSENSE_CLIENT=ca-pub-XXX を入れて再デプロイ → AdSense 管理画面で自動広告を ON（Jordan の作業）

### 2026-09-05 — 本文中の関連記事を最低2箇所にする自動差し込みを PostLayout に実装（72記事すべてで達成を実測・自動側の重複0件）。記事の広告枠を本文中2枠＋スマホ下部固定に拡張。CLAUDE.md 最上位ルール10 と blog-article スキルに記録

次のアクション:

- [ ] 忍者AdMAX の管理画面で本文中2枠（dailyhack-article-middle / mid2）のタグを作り src/data/admax-tags.ts に貼る

### 2026-09-05 — モーニング記事の X 告知を出した。画像 4 枚（1080×1080・rose テーマ・キャラは透かし）と 2 スレッド形式の本文をチャットで承認いただき、PR #366 をマージ。t048 が契約どおり（kind:thread / id は blog-promo- 始まり / image_path にカンマ区切り / thread_chain を run-publish.sh）で出す。あわせて gen-x-cards.mjs をブログの :root から引いた 5 テーマ化し、見出しの自動 1 行化とキャラの透かし表示を入れた。草案の出し先を Slack からこのチャットへ変更（最上位ルール 4 を改訂）。最上位ルール 9「待たなくていい時間を作らない」を追加。t046/t047 は main に入れ忘れていて一度も実行されていなかったのを同梱で解消。

次のアクション:

- [ ] t048 の実行結果（ops/heartbeat の reports/post-morning-thread.md）を読み、[1/2] と [2/2] が両方出たかを x_tweet_id で確認する。CDP が落ちていた場合はキューに積まれたままなので run-publish.sh を叩き直すタスクを出す。そのあと t046/t047 の結果を読み、アンフォロー 6 本の Bootstrap failed: 5 と候補供給の枯渇（フォロワー増の本当のボトルネック）に着手する。

### 2026-09-05 — IKEA記事の指摘2件に対応。表紙を全面タイル＋透過オーバーレイ形式に変更（gen-mosaic-hero に自動縮小を実装）、キャンペーン節を2番目に移して厚くした。スキルに両方記録

次のアクション:

- [ ] プレビューで確認してもらう

### 2026-09-05 — IKEA豊洲の記事を新規作成（ikea-toyosu-2026）。ゾーン別ガイド＋都心IKEAの変遷＋オープン記念キャンペーン。素材は069で取得（店内写真7枚・商品10枚・X4件・YouTube5本）

次のアクション:

- [ ] プレビューで確認してもらう。マージ後は X 告知を tweet2 に依頼

### 2026-09-05 — PR #336 をマージして本番公開完了（指摘23件）。X告知の依頼を新しい記事内容で書き直した

次のアクション:

- [ ] tweet2 の対応待ち。公開直後なのでGSCの再測定は1〜2週間後

### 2026-09-05 — 全文生成の方式は維持したまま、キャラ設定だけをテンプレ 37 件の実物から引き直す方針に訂正（方式ごとテンプレへ戻そうとして差し戻された）。t026 でテンプレ全文＋実際に出た返信 25 件を読む（$0）。x-reply-style スキルに §8 を追加

次のアクション:

- [ ] PR #344 マージ → 1分ポーラーが t026 を拾う → 実物の声を reply-style-prompt.json に入れ直す → 承認済み予算（上限5件 $0.021）で asuka-reply.cjs に書き下ろさせる

### 2026-09-05 — モーニング記事の見出し2件を差し替え、ランキング表を安い順に組み直し（順位列追加・吉野家430円=納豆定食を特定）。PR #336 をマージして本番公開へ

次のアクション:

- [ ] 公開後、GSCで順位を再測定する。ららぽーと記事にも「制作の裏側を書かない」を適用するか判断

### 2026-09-05 — モーニング記事の指摘5件を反映。モスの朝モスをプレスリリース経由で完全に埋めた（写真2枚・390円・Xの投稿）。内部向けの記述を全削除しスキルに記録。タグのデザイン修正

次のアクション:

- [ ] 10チェーン全部に写真が入った状態でプレビュー確認待ち

### 2026-09-04 — モーニング記事の指摘8件を反映完了。10チェーン中9チェーンにメニュー写真、10チェーン全部にロゴ。モスバーガーだけ朝モスの写真が公式に無く3回失敗したため写真なしで確定

次のアクション:

- [ ] 利用者のプレビュー確認待ち。次の指摘があれば対応する

### 2026-09-04 — モーニング記事の指摘8件中7件を反映（タイトル変更・30秒カード入替・10チェーンのロゴ・松屋の切れた写真を差し替え・表紙にロゴ）。066で残り4チェーンのメニュー写真を取得中

次のアクション:

- [ ] 066の結果からサンマルクカフェ・マクドナルド・モスバーガー・なか卯のメニュー写真を目視で選んで貼る。それで指摘8件が全部揃う

### 2026-09-04 — ららぽーと施設写真を10→16枚に（064の結果から6枚追加）。両記事の表紙を作り直し（見出しのはみ出し・チェーン名/施設名の札・サブタイトル）。モーニング記事の写真の置き場所と台帳も修正

次のアクション:

- [ ] 利用者がプレビューにコメント中。細かい指摘を受けて直す。写真が無いのは①TOKYO-BAY・⑥磐田・⑰福岡・⑱堺・⑲門真の5施設で、Commonsにも公式にも素材が無い

### 2026-09-04 — ららぽーと記事の施設節に写真を10枚入れた（Commons・出典/ライセンスを_manifest.jsonに記録）。残り10施設の候補取得タスク064を追加

次のアクション:

- [ ] 064の結果（30分以内）を見て残り10施設に写真を入れる。磐田・堺・門真は候補が無い可能性が高い

### 2026-08-31 — ららぽーとの残り施設に動画を足した（#321）。素材が入った施設は9→13/20。沼津は商圏の話、門真はパナの企業城下町の報道で、記事の数字に文脈がついた

次のアクション:

- [ ] 残り7施設（柏の葉・磐田・新三郷・和泉・海老名・立川立飛・堺）は施設そのものを扱った動画が見つからず。素材の側の問題でここから先は収穫逓減

### 2026-08-31 — X 自動化の全停止は tab-guard の正常動作だった。23:57:03 JST にタブが 19→1 枚になり、ai.openclaw.* が全 unload された（再起動ではない。uptime 20日）。返信・フォロー3件のみ t012 で復旧し、投稿系11件は意図的に停止のまま。経緯は docs/tab-guard-halt-2026-08-30.md。

次のアクション:

- [ ] (1) なぜタブが 19→1 になったかが未解明。23:57 JST 前後に何が動いていたかを調べる。(2) tab-guard の停止ロックが残っていないかを t015 で確認する。タブが戻っていない状態でロックを外すと再発火するだけなので外さない。

### 2026-08-31 — X 投稿遅延（2時間20分）の対策を全部入れ、実測で確認した。1分ポーラーを新設（t008→t010でパス誤りを修正）し、マージ→実行が実測46秒になった（前は最大1800秒）。publisher 契約を docs/x-publisher-contract.md に固定し、PR #298 の実調査で未確定3点を埋めた。二重投稿を防ぐため投稿タスク t002/t004/t005/t006/t007 を main から削除し、別セッションの PR #298 も閉じた。停止していた X 本線 3 件（comment-warmup / competitor-follower-follow / hashtag-follow）を t012 で復旧した。

次のアクション:

- [ ] 投稿系11件が未ロードのまま（意図的）。ジョブが14件一斉に落ちた原因は未特定なので、まず原因を調べてから戻すかを判断する。

### 2026-08-31 — GSC を実測して基準線を取った（8/1〜8/28）。ららぽーと 売上ランキングは7.5位のまま・表示60→69。打った手はこの窓の外なので効果はまだ測れない。新3記事はGSCに未出現

次のアクション:

- [ ] 9月上旬に再測定して、タイトル変更（8/29）と売上効率ランキング・目次修復・埋め込み追加（8/30）が効いたかを見る

### 2026-08-31 — 500円モーニング記事に YouTube 7本と X 2件を足した（#311）。YouTube 0→7本。マクドナルドは『朝10時20分から30分までは魔の時間帯』という投稿が記事の10:20締切の記述と噛み合った

次のアクション:

- [ ] 3記事とも各セクションに一次の声と映像が入った。次はGSCの実測を見て、打った手が効いているかを確認する

### 2026-08-31 — X 投稿が2時間20分出せなかった件の対策を全部入れた。1分ポーラー（t008）、実行の切り出し（ops-run-tasks.sh）、publisher 契約の固定、CLAUDE.md の15分ルール。二重投稿の芽を摘むため、実行済みの投稿タスク t002/t004/t005 を main から削除した。heartbeat のロックは、取れなくても押すのを優先する形に直した（STALE_MINUTES=90 に対し2回連続で押し損ねると誤報するため）。

次のアクション:

- [ ] t008 が走ってポーラーが入ったら、t009（実測タスク）をマージしてマージ→実行の実時間を測る。期待値は60秒以下。

### 2026-08-31 — サウナ告知が2時間20分かけて出せず、利用者が手で投稿した。t006/t007 は走る前に main から消して二重投稿を防いだ。原因分析を docs/x-post-latency-postmortem.md に、publisher の契約を docs/x-publisher-contract.md に固定した。

次のアクション:

- [ ] 対策 C（ハートビート3分化・安全な再ロード）と対策 D（即時経路）のどちらを入れるか、利用者の判断待ち。

### 2026-08-31 — ららぽーとの著名7施設（TOKYO-BAY/甲子園/豊洲/横浜/富士見/EXPOCITY/福岡）の節に X 4件・YouTube 7本を入れた（#300 #305）。記事全体で X10件・YouTube12本

次のアクション:

- [ ] 残りの施設（海老名・立川立飛・湘南平塚・沼津ほか）は未着手。次回のGSC実測で売上ランキングの順位が動いたかを見る

### 2026-08-30 — ららぽーとの各施設の節に X 4件と YouTube 5本を入れた（#300）。甲子園は西宮市長のレオポン投稿＋阪神パーク跡地の動画で、決算資料に無い切り口が入った

次のアクション:

- [ ] 残りの施設（横浜・豊洲・海老名・立川立飛など）にも同じことをやるかは未着手。サウナ記事は利用者確認済みでOK

### 2026-08-30 — ららぽーと記事に売上効率ランキングを新設（#295）。売上÷面積で、売上2位のTOKYO-BAYが効率7位に落ちることを出した。素材を取りに行かず記事内の未使用の数字を使った

次のアクション:

- [ ] 次回のGSC実測で『ららぽーと 売上ランキング』が7.5位から動いたかを見る。サウナは料金3施設が公式未発表で打ち止め

### 2026-08-30 — サウナ記事を17施設すべて写真つきに、料金を14施設に（#290）。公式で取れなかったものがプレスリリースには載っていた

次のアクション:

- [ ] サウナの残りは料金3施設のみ（SAUNA汽汽=予約側・蒸薪=公式が時間制としか書かない・門仲=未発表）。次はららぽーとの順位上げか新記事

### 2026-08-30 — サウナ記事の料金を13施設・写真を14施設に（#286）。荒木町Logoutは公式に到達して格上げ、水宴と海賊サウナは公式サイトが無いため報道を出典に明記

次のアクション:

- [ ] 残りは料金4施設（SHIAGARU・SAUNA汽汽・蒸薪・門仲。金額が予約カレンダー側にしか出ない）と写真3施設（荒木町Logout・おかえりサウナ板橋・黄金湯新宿）

### 2026-08-30 — 500円モーニング記事を公開（#280）。ららぽーとに X 実投稿4件（#274）。サウナの料金を11施設に（#275）。生 HTML の見出しが目次から落ちていたのを直した（#269）

次のアクション:

- [ ] tweet2 への X 告知依頼を出した。サウナの残り6施設の料金と、ららぽーと・サウナの写真なし施設の素材集めが残り

### 2026-08-30 — サウナ記事の料金表を9施設に拡充しPARADISEを税込で整列（#264）。ららぽーとにYouTube5本（#262）。500円モーニングの素材取得タスク049実行→時間は取れたが値段はJS描画で落ちたため051で取り直し。050でサウナ料金5施設＋ららぽーとX投稿8件を取得中

次のアクション:

- [ ] 051/050の結果を待って、500円モーニング記事を『何時まで・何がついてくるか』軸で執筆。写真は049の6枚中3枚（コメダ・すき家・上島）のみ使用可

### 2026-08-30 — ららぽーと記事を「売上ランキング」に寄せる改稿（タイトル/description/表紙/FAQ追加/公式リンク30行）PR #254 を作成。駐車場の「10施設」が実際は8施設だった誤りとセール節のビジュアル欠落も修正

次のアクション:

- [ ] PR #254 のマージ（GitHub コネクタが切断中でマージできず。復帰後に実施）

### 2026-08-29 — サウナ記事を17施設に拡張（舞浜ユーラシア・SAUNA汽汽）＋お風呂の年・目的別・手がけた人を追加 PR #249／お台場記事に6コンテンツとアクセスを追加 PR #250

次のアクション:

- [ ] PR #249・#250 をマージ→ SEO順位の確認と SLACK_WEBHOOK_URL 未登録の解消

### 2026-08-28 — お台場ドローンショー2026のまとめ記事を新規作成（全3公演・22回・中秋の名月の落とし穴）／文章の入る表を折り返すよう修正

次のアクション:

- [ ] PR #248 をマージ→ ops/tasks 036・037 が Mac で走る→ 参考記事の観点をサウナ記事へ取り込む

### 2026-08-28 — サウナ記事を15節に組み直し（表紙6枚タイル・X4件・YouTube11本・女性向けセクション）PR #247 マージ

次のアクション:

- [ ] 参考記事3本（asoview/TimeOut/ニフティ）の観点を取り込む＋お台場ドローンショー記事

### 2026-08-27 — 返信の文面ルールを x-reply-style スキルにした（CLAUDE.md ルール7）。032 で実際の返信文を持ち帰る。返信2倍(031)は ops/pending/ に置いて保留

次のアクション:

- [ ] 032 の結果を読んでテンプレ37件を直す→出力側トーン検査→そのあと量を上げる。#236(cap 5→10) は実績 0/5 なので効果なし、候補供給側が本当のボトルネック

### 2026-08-27 — 4 ループ稼働を確認（本日 24 件投稿・フォロワー 206→211）。売春系を種類で弾く NG 判定を作り comment-orchestrator に組み込んだ（#233/#234）。フォロー量を cap 5→10 に上げる（#236）

次のアクション:

- [ ] cap 引き上げ後の実測を見る（次の発火 11:30/18:30 JST）。029 の変換率が出たら『cap を X にすれば +Y 人/日』を数字で出す。実測 +1.25 人/日 では 9/30 に約 254 人で 46 人 不足

### 2026-08-23 — 4 ループを全部 稼働させた（①auto-thread-chainifier ②badge-followback ③auto-detect-and-unfollow-inactive ④competitor-follower-follow/hashtag-follow）。途中 plutil -extract を -o 無しで実行して plist 56 件を破壊し、launchctl print のメモリ上設定から再生した

次のアクション:

- [ ] 壊れたまま残っている plist 53 件（import-manual-image / slack-watchdog / tab-guard など）を launchctl print から再生する。Mac を再起動すると稼働中 15 件が全部止まるため優先度が高い。SLACK_WEBHOOK_URL の登録は Jordan の手が 1 回必要

### 2026-08-22 — 掲載順位を必ず報告する経路を作った（seo-rankings.py / ops task / seo-monitoring.md にルール化）。週次レポートの「惜しい記事（6〜20位）」フィルタが1〜5位を構造的に除外していたのが原因。PR #195

次のアクション:

- [ ] PR #195 をマージし、Mac が ops/tasks/006 を実行して reports/seo-rankings.md を push するのを待って実測順位を報告する
### 2026-08-22 — フォロワー目標を 9/30 まで 300 人に再設定するタスク(006)と、会話継続ジョブを調べるタスク(007)を投入。フォロー返し(badge-followback)が実機のジョブ一覧に載ったことを確認（12→16件）

次のアクション:

- [ ] 007 の結果（chainifier の発火間隔と上限）を見て月額を出し、ロードするか判断する。アンフォロー用スクリプトは plist が無く一度も動いていないため、同様にまず調べる

### 2026-08-16 — フォロワー増加の4ループ（会話継続・フォロー返し・アンフォロー・能動フォロー）を docs/x-engagement-loop.md に仕様化。heartbeat に実投稿数/reloaders/認証状態を追加し、検出条件を実機のログ書式に修正。Slack が2チャンネルあること・依頼はトリガー経路のみ有効なことを記録

次のアクション:

- [ ] Mac から follower-snapshot 復旧と ①会話継続の DRY_RUN 結果を受ける。①が1週間無事なら ②フォロー返し → ④能動フォロー → ③アンフォロー の順に依頼

### 2026-08-16 — blog2 を誤ってアーカイブし復元(unarchive)。生きており tweet2 が OpenClaw 復活の依頼先に使う予定だった。返答は Slack ではなく PR で届いていた

次のアクション:

- [ ] tweet2 の create_trigger 承認待ちを解消する（利用者が tweet2 セッションで承認）。OpenClaw 復旧は tweet2 の担当

### 2026-08-16 — daily-hack-blog2 をアーカイブ（応答が数日無く実行手段として機能していなかったため）。会話は Mac の JSONL と session-log/blog2 に残存

次のアクション:

- [ ] Mac 側の実行が必要になったら claude --resume 7d5942fa-... → /remote-control で口を作り直す

### 2026-08-16 — blog3(記事)とtweet2(X運用)の役割分担・所有権・依頼表を仕組み化し PR #172 を作成。session-start フックが未決依頼を注入する

次のアクション:

- [ ] PR #172 をマージ後、PR #169(会話ログ回収)に main を取り込んでマージ判断

### 2026-08-16 — 反復作業のコスト提示を最上位ルール化（2-B）し毎ターン注入。実測コストを docs/recurring-job-costs.md に記録。緊急依頼が埋もれた件のプロトコルを docs 化。Mac に 2picks 再開と15分リプ着手を承認

次のアクション:

- [ ] Mac から (1) 2picks 変更後の launchctl list (2) 15分リプの実装状況 の報告を受ける

### 2026-08-13 — OpenClaw 停止の原因を実機で特定（ジョブ未ロード／握り潰された空例外）。誤った前提3件を撤回し記録を訂正。画像の場所をSlackで訂正

次のアクション:

- [ ] OpenClaw が main から画像をアップロードするか確認し、👍 を得てから X 投稿

### 2026-08-11 — ダイアログ回避（検知語の言い換え）を塞ぎ両リポジトリに反映。OpenClaw 復旧手順を docs 化

次のアクション:

- [ ] home-mac で docs/openclaw-recovery.md の手順を実行し、MUST rule 復元→画像添付→👍→X投稿

### 2026-08-11 — ダイアログ強制の拡張を両リポジトリに反映（daily-hack #152 / bubblesnow #2 をマージ）

次のアクション:

- [ ] home-mac 復旧後に OpenClaw が画像を Slack へ添付し、承認を得て X へ投稿

### 2026-08-11 — ダイアログ強制を「疑問符」だけでなく「次の一手の提示」まで拡張。本文で残件を並べる形の見逃しを回帰テスト化

### 2026-08-11 — API課金ルールを「禁止」から「見積もり→提示→許可」の運用に修正。誤検知バグを修正し BubblesNow にも移植

次のアクション:

- [ ] BubblesNow の PR をレビューしてマージ

### 2026-08-11 — API課金の事前確認を最上位ルール2として記録し、PreToolUseフックで強制（テスト20件）

### 2026-08-11 — SessionStart フックの注入を要約のみに変更（履歴を除外、61%削減）。追記でキャッシュが失効する構造を解消

### 2026-08-11 — OpenClaw 不具合の原因を特定: 8/10 09:00 に MUST rule 7件が消失し復元失敗。verify_external_state_before_claiming が失われたことが虚偽報告の原因

次のアクション:

- [ ] home-mac で MUST rule を復元（旧ミラー MEMORY-MUST-MIRROR.md を確認）
- [ ] spawnSync ETIMEDOUT の解消と launchd ジョブの生存確認
- [ ] OpenClaw 復旧後に画像添付→承認→X投稿

### 2026-08-11 — OpenClaw の画像アップロード完了報告が虚偽と判明（f5771c7 はリモートに無く、スレッドに添付も無い）。Slackで訂正投稿

次のアクション:

- [ ] OpenClaw が実ファイルを添付するまで 👍 を出さない
- [ ] 添付されない場合は別の受け渡し手段を検討

### 2026-08-11 — Slack操作を全面許可。settings.json に mcp__Slack__* を登録し CLAUDE.md にも記録

### 2026-08-11 — 指定フォーマット（親＋[n/N]返信）でX投稿草案をSlackに再送。旧投稿には無視する旨を追記

次のアクション:

- [ ] OpenClaw の画像アップを待ち、Jordan の 👍 を得る

### 2026-08-11 — ランキング画像の共有依頼をSlackに新規投稿（OpenClawにアップロード依頼／承認前のX投稿を禁止と明記）

次のアクション:

- [ ] OpenClaw が画像をアップしたら Jordan の 👍 を待つ
- [ ] 👍 後に OpenClaw が X へ投稿

### 2026-08-11 — ランキング画像はX投稿用と確認。公開される public/ から assets/social/ へ退避（記事は未変更）

次のアクション:

- [ ] OpenClaw に画像アップを依頼して承認を得る
- [ ] 承認後 OpenClaw が X に投稿

### 2026-08-11 — 全国マップ画像は不要との判断により削除。SNS画像は売上ランキング1枚に確定

次のアクション:

- [ ] ランキング画像1枚をSlackで共有し承認を得る
- [ ] 承認後 OpenClaw に X 投稿を依頼

### 2026-08-11 — ランキング画像に公式ららぽーとロゴを右上配置（提供素材を余白除去して使用）

### 2026-08-11 — 依頼された2枚（売上ランキング正方形／全国マップ）を作成しブランチに保存

次のアクション:

- [ ] Slackへの共有方法を決める（リポジトリ経由で公開URL化 or チャット確認のみ）
- [ ] 承認後に OpenClaw へ X 投稿を依頼

### 2026-08-10 — 選択肢をダイアログで出すルールをフックで強制する仕組みを追加（予防＋差し戻しの二層、テスト12件）

### 2026-08-10 — OpenClaw 連携の前提（X 投稿は OpenClaw 経由・Slack 確認フロー）を CLAUDE.md に復元

### 2026-08-10 — Slack コネクタが Taxa ワークスペースに接続されており fieldbeside を読めないことが判明

次のアクション:

- [ ] Slack コネクタを fieldbeside.slack.com で認証し直す
- [ ] 認証後、依頼された画像の要件をスレッドから読み取って作成する
- [ ] 画像を Slack に投稿し、確認を得てから X に投稿する

### 2026-08-10 — 選択肢はダイアログで出すルールを CLAUDE.md の最上位に記録

### 2026-08-10 — セッション断絶に備えた引き継ぎ機構を追加

次のアクション:

- [ ] MOP記事の扱いを決める
- [ ] 決定後に記事を作成しビルド確認

### 2026-08-10 — 環境消滅によるセッション断絶への対応

- `daily-hack-tweet` / `daily-hack-blog` セッションが `environment_deleted` で操作不能になった。
  いずれも `environment_kind: bridge`（ローカル CLI を Remote Control で Web に公開したもの）。
- 会話履歴自体はローカルの `~/.claude/projects/` に残るため、`claude --resume` で継続可能。
- 再発対策として、この引き継ぎファイルと SessionStart フックを追加した。
- 消滅時点で判明していた各セッションの状態:
  - `daily-hack-tweet`: 「draft resent; auto-recovery enabled, notifications hardened」
  - `daily-hack-blog`: 「badge text color fixed; white now renders on prod」
  - `daily-hack-blog`（別セッション）: MOP 記事の扱いを問う質問で停止中

<!-- session-log:end -->
