# 保留中のタスク

**ここに置いたスクリプトは実行されない。** `ops-heartbeat.sh` が拾うのは
`ops/tasks/*.sh` だけ（`scripts/ops-heartbeat.sh:153`）。

書き上がっているが、**前提が揃うまで走らせたくない**ものをここに置く。
消してしまうと書き直しになるので残す。**走らせるときは `ops/tasks/` へ動かす。**

| ファイル | 何をする | 走らせる条件 |
| --- | --- | --- |
| `031-double-replies.sh` | 返信を 8 → 16 件/日（`MAX_PICKS_PER_FIRE` 2 → 4） | **返信の文面を直し、出力側のトーン検査を入れてから。** 危ない文のまま倍にしても本数が増えるだけ（`.claude/skills/x-reply-style/SKILL.md` §5） |
| `034-stop-referral-codes.sh` | 危険テンプレ T03/T06 を外す＋闇バイト語を NG に足す＋出口検査の差し込み位置を調べる | **利用者の判断待ち。** テンプレを実機から外す＝返信の挙動が変わるため、勝手に走らせない |
